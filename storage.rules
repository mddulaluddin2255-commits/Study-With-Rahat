rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can read public educational images and documents
    match /{allPaths=**} {
      allow read: if true;
      // Only authenticated users / administrators can write
      allow write: if request.auth != null;
    }
  }
}
